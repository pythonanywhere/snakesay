[tool.poetry]
name = "snakesay"
version = "0.9.0"
description = "speaking snake"
authors = ["PythonAnywhere Developers <developers@pythonanywhere.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = ">=3.7"

[tool.poetry.dev-dependencies]
pytest = "^7.1.1"
pytest-cov = "^3.0.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
